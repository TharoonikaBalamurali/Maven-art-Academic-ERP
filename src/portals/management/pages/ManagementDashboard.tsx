/**
 * Route target for `/management`.
 *
 * The dashboard is a self-contained feature slice; the portal only routes to
 * it. The role-specific composition lives in `@/features/dashboard`.
 */
export { ManagementDashboard } from '@/features/dashboard/components/ManagementDashboard';
